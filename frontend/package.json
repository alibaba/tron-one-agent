{
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "yarn workspace control dev ",
    "build": "yarn workspace control build",
    "format": "prettier --write 'packages/**/*.{ts,tsx,js,jsx,json,less,css}'"
  },
  "devDependencies": {
    "prettier": "^3.8.3"
  }
}
